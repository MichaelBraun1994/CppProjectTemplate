# Ensure that file endings are consistent between Windows and Linux to prevent unnecessary changes tracked by git.
* text=auto eol=lf
*.{cmd,[cC][mM][dD]} text eol=crlf
*.{bat,[bB][aA][tT]} text eol=crlf